export interface Property {
  id: number;
  title: string;
  location: string;
  price: string;
  description: string;
  image: string;
  features: string[];
}

export interface NavItem {
  label: string;
  href: string;
}
